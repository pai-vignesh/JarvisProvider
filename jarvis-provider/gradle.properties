kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true

# Plugin API versioning — bump minApiVersion when breaking changes are introduced.
jarvis.minApiVersion=1
jarvis.targetApiVersion=1

# Minimum Jarvis app version that can load these plugins.
jarvis.minAppVersion=2.0.0
